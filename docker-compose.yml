services:
  front:
    build:
      context: .
      args:
        # Se o backend rodar em outro container/host, ajuste essa URL.
        VITE_API_URL: http://localhost:8000
    ports:
      - "5173:80"
